{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "customers": {
      ".read": "auth.uid !== null",
      ".write": "auth.uid !== null"
    }
  }
}
