{
  "rules": {
    ".read": "auth.uid !== null",
    "users": {
      "$uid": {
        ".write": "auth.uid === $uid"
      }
    },
    "customers": {
      ".write": "root.child('users').child(auth.uid).child('division').val() === 'sales'"
    },
    "orders": {
      ".write": "auth.uid !== null"
    }
  }
}
