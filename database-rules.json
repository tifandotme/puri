{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth.uid === $uid",
        ".write": "auth.uid === $uid"
      }
    },
    "customers": {
      ".read": "auth.uid !== null",
      ".write": "root.child('users').child(auth.uid).child('division').val() === 'sales'"
    }
  }
}
