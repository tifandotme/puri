{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  //
  // https://firebase.google.com/docs/emulator-suite/install_and_configure#startup
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "command": "firebase emulators:start --only auth,database --import=./firebase-emulator --export-on-exit",
      "label": "firebase: emulators",
      "detail": "start firebase emulator (auth and database only)",
      "problemMatcher": []
    }
  ]
}
